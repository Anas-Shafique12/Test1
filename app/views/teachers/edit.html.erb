<h1>Edit Teacher</h1>

<%= form_with model: @teacher, local: true do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', teachers_path %>
